version: '3.8'

services:
  # MariaDB Database
  db:
    image: mariadb:10.11
    container_name: property-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${DB_NAME:-property_management}
      MYSQL_USER: ${DB_USER:-propman}
      MYSQL_PASSWORD: ${DB_PASSWORD:-change_this_password}
      TZ: Europe/Bucharest
    command: --default-time-zone=Europe/Bucharest
    volumes:
      - db_data:/var/lib/mysql
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
      # Uncomment the line below to load development seed data (demo tenants, invoices, etc.)
      # - ./database/seeds/development.sql:/docker-entrypoint-initdb.d/02-seed.sql:ro
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - property-network

  # Perl Dancer2 Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: property-backend
    restart: unless-stopped
    environment:
      TZ: Europe/Bucharest
      DANCER_ENVIRONMENT: ${ENVIRONMENT:-docker}
      # Database
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-property_management}
      DB_USER: ${DB_USER:-propman}
      DB_PASSWORD: ${DB_PASSWORD:-change_this_password}
      DB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      # Security secrets
      JWT_SECRET: ${JWT_SECRET:-change_this_to_a_very_long_random_string_in_production}
      SESSION_COOKIE_KEY: ${SESSION_COOKIE_KEY:-change_this_session_key_in_production}
      CSRF_SECRET: ${CSRF_SECRET:-change_this_csrf_secret_in_production}
      # Google OAuth
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-http://localhost:5001/api/google/callback}
      # Application URLs
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      # Rate limiting
      RATE_LIMIT_MAX_ATTEMPTS: ${RATE_LIMIT_MAX_ATTEMPTS:-5}
      RATE_LIMIT_WINDOW_SECONDS: ${RATE_LIMIT_WINDOW_SECONDS:-900}
    volumes:
      - ./backend:/app
      - pdf_temp:/tmp/property_invoices
    ports:
      - "5001:5000"  # External:Internal - use 5001 to avoid conflict with local dev
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - property-network

  # React Frontend (Nginx)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: property-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - property-network

volumes:
  db_data:
    driver: local
  pdf_temp:
    driver: local

networks:
  property-network:
    driver: bridge
